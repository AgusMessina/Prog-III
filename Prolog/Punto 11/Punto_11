%mayorN(+N, +L, ?LR).
mayorN(_,[],[]).
%Caso Base: Se cumple cuando la lista L y la lista que recorremos dejan de tener elementos disponibles para buscar

mayorN(N, [X|Y], [X|LR]):- X > N, mayorN(N, Y, LR).
%Si el primer elemento de L es mayor a N, entonces lo "pasamos" a LR, para despues seguir recorriendo

mayorN(N, [X|Y], LR):- X < N, mayorN(N, Y, LR).
%Si el primer elemento de L es menor a N, entonces el codigo no hace nada y sigue recorriendo

mayorN(N, [N|Y], LR):- mayorN(N, Y, LR).
%Si el primer elemento de L es igual a N
